// Sorting a string in alphabetical order

const sortArray = (sampleArray) => {
  let sortedArray = [];
  sortedArray = sampleArray.sort();
  return sortedArray;
}

let shuffledArray = ['kilo', 'alpha', 'beta', 'lima', 'romeo']
console.log(sortArray(shuffledArray))